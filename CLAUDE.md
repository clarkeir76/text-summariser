# text-summariser

## Git Workflow

This is a **solo repository** — there is only one contributor.

- Commit and push **directly to `main`**. Do not create branches or PRs.
- When using `/bpp-commit`, skip the branch safety guard — committing to `main` is correct and intended.
